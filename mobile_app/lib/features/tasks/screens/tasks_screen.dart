import 'package:flutter/material.dart';
import 'package:mobile_app/features/assignments/presentation/screens/assignments_list_screen.dart';

/// Global Tasks Screen - Shows all assignments across all subjects
class TasksScreen extends StatelessWidget {
  const TasksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const AssignmentsListScreen();
  }
}

